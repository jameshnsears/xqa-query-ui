update README, so I don't have to look in this file!

move all data / layouts to reflect xqa data / domain objects

look at https://github.com/expressjs/express for interesting ideas

i18n

---

variables only defined at class level

---

install:

npm install -g @angular/cli 
npm install -g eslint

npm install primeng --save 
npm install @angular/animations --save
npm install font-awesome --save
npm install chart.js --save

---

panic:

rm -rf ~/.npm ~/.node-gyp

ng build
ng serve --open

---

update:

rm -f package-lock.json && npm install

npm update -D && npm update -S
npm outated

npm install -g npm-check-updates
ncu 
ncu -u

---

code:

ng generate component performance
ng generate component search
ng generate component status
ng generate component xquery

ng generate service car --module=app

---

https://en.wikipedia.org/wiki/Swagger_(software)

---

icons: https://fontawesome.com/icons?m=free

---

e2e: http://www.protractortest.org/#/toc

---

npm install  # if newly cloned
ng serve --open

---

=>

In a type position, => defines a function type where the arguments are to the left of the => 
and the return type is on the right. So callback: (result: string) => any means 
"callback is a parameter whose type is a function. That function takes one parameter called 
result of type string, and the return value of the function is of type any".

// Old-school:
var a2 = a.map(function(s){ return s.length });

// ECMAscript 6 using arrow functions
var a3 = a.map( s => s.length );

---

https://github.com/primefaces/primeng/tree/master/src/app/showcase

---
